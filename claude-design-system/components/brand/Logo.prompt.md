Official Universidad de La Sabana horizontal logo — renders the supplied brand asset, use `color` on light backgrounds and `white` on dark/photographic ones.

```jsx
<Logo variant="white" height={56} basePath="../assets" />
```

Variants: `color` (positive, PNG) · `white` (negative, PNG) · `mono` (black SVG, scalable). `height` is floored at 22px per the brand book. Never recolour, stretch, add stroke, or reposition the mark.
