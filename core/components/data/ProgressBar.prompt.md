Labelled progress / share bar in the active accent, over a soft accent track.

```jsx
<ProgressBar label="Ejecución presupuestal" value={72} />
```

Props: `value` (0–100), `label`, `showValue`, `height`. Colour tracks `--accent`.
