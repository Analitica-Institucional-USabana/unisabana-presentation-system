White content surface with soft shadow and optional accent rule.

```jsx
<Card accent="top" padding={28}>
  <Eyebrow>Programa</Eyebrow>
  <h3>Maestría en …</h3>
</Card>
```

Props: `accent` (`none|top|left` — 3px `--accent` rule), `padding`.
