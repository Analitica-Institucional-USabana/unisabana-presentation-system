Small pill label for categories, statuses or metadata.

```jsx
<Tag tone="accent">Investigación</Tag>
<Tag tone="cream">2024</Tag>
```

Tones: `accent` (soft accent tint, default), `solid`, `neutral`, `cream`.
